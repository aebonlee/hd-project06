# [Claude Code 작업 지시서] 지게차 엔진 사양 관리 및 비교 웹페이지

- 기획자: 홍근영
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 첨부: `engine_curve_R1.zip` (11KB) — 기획자가 일부 기능을 직접 구현한 시제품. **작업 시작 전 이 zip을 전달받아 압축을 풀고 먼저 읽어볼 것** (이 문서에는 포함되어 있지 않음)
- 프로젝트 유형: 엑셀 Import/Export 기반 데이터 관리 + 인터랙티브 차트(출력·토크 곡선) + 최대 4개 엔진 비교

---

## 0. Claude Code에게 — 작업 방식

1. 기획자가 만든 `engine_curve_R1.zip`이 있으면 **그 코드를 기반으로 확장**한다(구조가 나쁘지 않다면 다시 만들지 말 것). 없으면 새로 만든다.
2. **사내 대외비 데이터**이므로 외부 AI API를 호출하는 코드는 넣지 않는다. "사양서 → 엑셀 정리" 단계(3.1)는 사내 Copilot으로 수행하는 것이 전제이므로, 이 프로젝트에서는 **Copilot에게 줄 프롬프트 + 엑셀 표준 양식**을 산출물로 제공하고, 웹페이지는 그 엑셀을 Import하는 것부터 담당한다.
3. 데이터베이스는 **엑셀 파일이 기준**이다(Import 양식 = Export 양식 완전 동일). 서버 없이 브라우저만으로 동작하는 정적 HTML을 목표로 한다.
4. 항목 추가(예: 엔진 보증 수명)가 코드 수정 없이 가능해야 한다 → 스키마를 데이터로 관리한다.
5. 기획서의 번호 체계(3.1 ~ 3.3, 3.2.1.1 ~ 3.2.2.2)를 요구사항 ID로 그대로 사용한다.
6. 부록의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

엔진사별로 제각각인 엔진 사양서에서 필요한 제원을 **표준 엑셀 양식**으로 정리해 관리하고, 엔진 모델명 또는 지게차 모델명으로 조회하면 제원 표와 **RPM별 출력·토크 곡선 그래프**를 보여주며, **최대 4개 엔진을 나란히 비교**할 수 있는 HTML 페이지.

## 2. 배경 및 현재 업무 상황

- 신규 지게차 모델 개발 시 엔진 업체로부터 사양서를 받는데, 업체별로 양식과 파일 형식(PDF/PPT/엑셀)이 모두 다름.
- 설계 담당자가 필요한 스펙만 골라 관리용 엑셀에 **수기 입력**하느라 시간이 오래 걸림.
- 정보는 사내 대외비 → 사내 Copilot만 사용 가능.

## 3. 요구 기능

### 3.1 사양서 → 엑셀 데이터베이스 (Copilot 활용 전제)
- 3.1.1 외부 AI 금지. 사내 Copilot용 **추출 프롬프트 템플릿**과 **표준 엑셀 양식** 제공.
- 3.1.2 관리 항목 추가 가능(예: "엔진 보증 수명" 추가) → 양식에 열 추가만으로 웹페이지가 인식.
- 3.1.3 엑셀 **Import** 기능(지정 양식).
- 3.1.4 엑셀 **Export** 기능 — Import 양식과 완전 동일 → 다른 PC에서도 조회·관리 가능.

### 3.2 조회 화면
- 엔진 모델명 또는 적용 지게차 모델명으로 검색/선택 → 해당 엔진 제원을 가독성 있는 표로 표시(4개 그룹: 기본 정보 / 성능 / 주요 제원 / 규제·인증).
- 3.2.1 성능 스펙은 **RPM별 출력 및 토크 그래프**:
  - 3.2.1.1 꺾은선 그래프, 각 데이터 지점을 점으로 표시.
  - 3.2.1.2 점 클릭 시 값 표시 (예: `70kW @ 2300 rpm`).
  - 3.2.1.3 정격 출력 및 최대 토크는 그래프상에 **항상 값 표시**(주석 라벨).
  - 3.2.1.4 Y축 범위 조정 가능(0~100, 50~100 등) — 출력/토크 축 각각.

### 3.3 엔진 비교 (최대 4개)
- 3.2.2.1 엔진 1개당 1개 컬럼. 왼쪽 엔진과 값이 동일하면 **"<-"** 로 표기.
- 3.2.2.2 출력·토크 그래프를 **겹쳐서** 표시, 엔진별 색상 구분.

## 4. 데이터 스키마 (표준 엑셀 양식 — 샘플 생성 지침)

**시트 1 `engines`** (엔진 1행)

| 그룹 | 항목 |
|---|---|
| 기본 정보 | 엔진 제조사, 엔진 모델명, 적용 지게차 모델(복수, 쉼표 구분) |
| 성능 | 정격 출력(kW), 정격 출력 RPM, 최대 토크(Nm), 최대 토크 RPM, Low Idle RPM, High Idle RPM |
| 주요 제원 | 무게(kg), 엔진 타입(기계식/전자식), 실린더 수, 배기량(cc), 오일 용량(L), 냉각수 용량(L), 연비, 알터네이터 스펙, 스타터 모터 출력 |
| 규제/인증 | 배기 규제 인증, 후처리장치 타입(DPF 등) |

**시트 2 `curves`** (RPM별 곡선, 엔진당 여러 행): 엔진 모델명, RPM, 출력(kW), 토크(Nm)

**시트 3 `schema`** (선택): 항목 키, 표시명, 그룹, 단위, 순서 — 이 시트로 항목 추가/순서를 관리하면 3.1.2 충족.

샘플: 제조사 3곳, 엔진 6개, 엔진당 RPM 포인트 8~12개.

## 5. 제약사항 및 가정

- 외부 네트워크·외부 AI 호출 없음. 라이브러리는 CDN 대신 **로컬 파일로 동봉**(사내망 고려).
- 브라우저 단독 실행(정적 HTML + JS). 데이터는 엑셀 Import 후 메모리/LocalStorage에 보관, Export로 영속화.
- Import/Export 양식 동일성은 자동 테스트로 보장(Import → Export → 재Import 시 데이터 동일).

## 6. 권장 기술 스택 (제안)

- HTML/CSS/JavaScript 단일 페이지(또는 소수 파일).
- 엑셀: SheetJS(xlsx) — 로컬 동봉.
- 차트: Chart.js(+ annotation 플러그인으로 정격/최대 라벨 상시 표시) 또는 ECharts. 이중 Y축(출력/토크), 클릭 툴팁, 축 범위 입력 UI.
- 기획자의 `engine_curve_R1` 구현이 특정 라이브러리를 쓰고 있으면 그것을 유지.

## 7. 산출물

1. 표준 엑셀 양식 파일(빈 양식 + 샘플 데이터 채운 버전)
2. 사내 Copilot용 "사양서 → 표준 양식" 추출 프롬프트 템플릿(.md)
3. 웹페이지(조회 / 그래프 / 비교 / Import / Export)
4. 양식 동일성 테스트
5. README(사용법, 항목 추가 방법, 사내 배포 방법 — 파일 공유 폴더에 두고 열기)

## 8. 구현 단계

- Phase 0: `engine_curve_R1.zip` 분석 및 재사용 범위 결정
- Phase 1: 표준 엑셀 양식 + 샘플 데이터 + Import/Export + 엔진 목록·검색
- Phase 2: 제원 표(4그룹) + 출력·토크 그래프(점 클릭, 상시 라벨, Y축 범위)
- Phase 3: 최대 4개 비교(컬럼 표 + "<-" 동일 표기 + 겹친 그래프)
- Phase 4: 스키마 시트로 항목 추가 지원, Copilot 프롬프트, README

## 9. 확인 필요 사항 (작업 전 질문)

1. `engine_curve_R1.zip`을 제공해 줄 수 있는가? 어떤 기술로 만들었는가?
2. 출력·토크 그래프를 한 차트(이중 Y축)로 볼 것인가, 두 차트로 나눌 것인가?
3. "<-" 동일 표기 시 수치 비교 허용 오차가 있는가(예: 무게 ±1kg도 동일로 볼지)?
4. 데이터 영속화: 엑셀 Export만으로 충분한가, 브라우저 LocalStorage 자동 저장도 원하는가?
5. 항목 추가 방식: 엑셀 열 추가만으로 충분한가, 웹 화면에서 항목 추가 UI도 필요한가?
6. 사내 배포 방식(공유 폴더의 HTML 파일 열기 / 사내 웹서버)?

---

## 부록. 기획서 원문 (Padlet 게시 내용 그대로)

홍근영 (엔진 사양관리 기획)
[첨부: engine_curve_R1.zip, 11KB]

1. 목적
- 지게차 엔진 정보를 종합적으로 관리하고, 시각화하여 설계담당자가 엔진 정보를 손쉽게 검토할 수 있는 html 페이지를 만들고자 함

2. 업무 상황
- 신규 모델 개발을 할 때, 엔진 업체로부터 엔진 사양서를 받고, 사양서 내부에는 엔진의 상세 제원이 표시되어있음.
- 이 제원 중, 신규 개발시 필요로 하는 주요 스펙들을 하기와 같이 정리하는데, 엔진사에서 제공하는 사양서는 업체별로 양식이 매우 상이하고 PDF, PPT, 엑셀 등, 파일 형식도 달라서, 스펙들을 정리하려면 설계 담당자들이 관리하는 엑셀 파일에 수기로 입력해야므로 시간이 오래걸림

<엔진 제원 목록>
1. 기본 정보: 엔진 제조사, 엔진 모델명, 적용 지게차모델
2. 성능 관련 스펙: RPM별 출력, RPM별 토크, 정격 출력 및 최대토크, LOW/HIGH Idle RPM
3. 주요제원: 무게, 엔진 타입(기계식/전자식), 실린더 수, 배기량, 오일용량, 냉각수 용량, 연비, 알터네이터 스펙, 스타터 모터 출력
4. 규제/인증 관련: 배기 규제 인증, 후처리장치 타입 : DPF

3. 목표 기능
3.1. 엔진사에서 제공하는 일관성 없는 사양서를 AI에 업로드하면, AI가 우리가 필요한 정보만을 골라서 엑셀파일 기반 데이터베이스로 정리
3.1.1. 해당 정보들은 사내 대외비 정보이므로, 사내에서 쓰는 copilot만을 활용해야함.
3.1.2. 관리해야하는 데이터 베이스의 항목이 추가되는 경우를 고려해야함. (예를 들어 엔진 제원 목록 중 엔진보증수명을 항목으로 추가하는 경우 등)
3.1.3. 엑셀파일 기반 양식을 지정하여, 엑셀 Import 기능이 있어야함
3.1.4. 현재까지 저장된 엔진 사양 데이터를 다른 컴퓨터에서도 조회하고, 관리할수있도록 엑셀 데이터 export (양식은 import 와 완전 동일) 기능이 있어야함.
3.2. 데이터베이스화한 정보들을 바탕으로, 엔진 모델명 또는 지게차 모델명을 기준으로 조회하면, 해당 엔진 정보를 전반적으로 가독성있게 표 등으로 표현
3.2.1. 엔진 정보의 성능 관련 스펙은 RPM별 출력 및 토크를 그래프로 표현해야함.
3.2.1.1. 출력 및 토크 그래프는 꺾은선 그래프이고, 각 값이 적힌 지점을 점으로 표현
3.2.1.2. 각 지점을 클릭하면 해당 값이 표시되도록 구현 (ex. 70kW @ 2300 rpm)
3.2.1.3. 정격 출력 및 최대 토크는 그래프상에서 항상 값이 표시되도록 구현
3.2.1.4. 그래프의 Y축은 나타내는 범위를 조정할 수 있도록 함 (0~100일지, 50~100일지 등)
3.3. 최대 4개 까지 엔진 사양을 비교할 수 있도록 구현
3.2.2.1. 엔진을 비교할때, 1개 엔진 제원당 1개의 컬럼으로 표현, 엔진 제원이 동일하다면, "<-" 으로 왼쪽과 동일하다고 표기
3.2.2.2. 엔진을 비교할때, 엔진 출력 및 토크 그래프는 그대로 겹쳐지게 표현, 다만 색상은 다르게 표현한다.

유첨파일에 해당 기능을 일부 구현하였으니 참고해주시면 감사하겠습니다.
